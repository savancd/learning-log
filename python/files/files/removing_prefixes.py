#   Common task is to remove a prefix.
#   When the user enter into addres bar you should remove the prefix so the user can focus only on the part without prefix

dogs_url = "https://dogs.com"

print(dogs_url.removeprefix("https://"))    #   removes the prefix or any other part that is in the string.removeprefix("HERE")


